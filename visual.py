from vpython import *

#plot1 = gcurve(color = color.white)

#for x in arange(0., 8.1, 0.1):
#    plot1.plot(pos = (x, 5.*cos(2.*x) * exp(-0.4 * x)))

graph1 = graph(width = 500, height = 500, title = '2D', range = 10)

sphere(vector = (0, 0, 0), radius = 1, color = color.green)
